import MemberRoleTable from "@/components/members/MemberRoleTable";

/**
 * 회원 페이지: 회원 목록/역할 관리(SUPER_ADMIN 전용 변경).
 */
const MembersPage = () => {
  return <MemberRoleTable />;
};

export default MembersPage;
